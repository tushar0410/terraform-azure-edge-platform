# Modules

Reusable Terraform modules will be introduced here one Azure capability at a time. Every module will own a focused concern, expose validated inputs and useful outputs, and include its own README with architecture, deployment, and test guidance.

No modules are present in this initial repository commit. The first planned module is `resource-group`.
