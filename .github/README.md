# GitHub configuration

Repository automation and contribution controls live here. Workflows will be added only after Terraform roots and backend conventions are executable.

The intended delivery model is pull-request validation, reviewed plans, and approval-gated applies using GitHub Actions OpenID Connect rather than stored Azure client secrets.
